buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:2.1.0'
    }
}

apply plugin: 'java-library'
apply plugin: 'groovy'
apply plugin: 'kotlin'

repositories {
    google()
    mavenCentral()
}

java {
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
}

tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).configureEach {
    compilerOptions.jvmTarget.set(org.jetbrains.kotlin.gradle.dsl.JvmTarget.JVM_17)
}

dependencies {
    compileOnly gradleApi()
    compileOnly localGroovy()
    compileOnly 'com.android.tools.build:gradle:8.13.0'
    implementation 'com.squareup:javapoet:1.13.0'
    implementation 'com.android.tools.build:gradle:8.13.0'
    implementation 'commons-io:commons-io:2.8.0'
    implementation 'commons-codec:commons-codec:1.15'
    implementation 'org.javassist:javassist:3.28.0-GA'
}

sourceSets {
    main {
        kotlin.setSrcDirs([])
        groovy.srcDirs = ['src/main/groovy', '../auto-service-plugin/src/main/groovy']
        resources.srcDirs = ['src/main/resources', '../auto-service-plugin/src/main/resources']
    }
    pluginEntry {
        kotlin.srcDirs = ['../auto-service-plugin/src/main/kotlin']
        compileClasspath += files(
                layout.buildDirectory.dir('classes/groovy/main'),
                layout.buildDirectory.dir('classes/java/main'),
                layout.buildDirectory.dir('resources/main')) + configurations.compileClasspath
        runtimeClasspath += output + compileClasspath
    }
}

configurations {
    pluginEntryImplementation.extendsFrom implementation
    pluginEntryCompileOnly.extendsFrom compileOnly
}

tasks.named('compilePluginEntryKotlin') {
    dependsOn tasks.named('compileGroovy')
}

tasks.named('classes') {
    dependsOn tasks.named('pluginEntryClasses')
}

tasks.named('jar') {
    from sourceSets.pluginEntry.output
}
