pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url = uri('test-repo') }
    }
}

rootProject.name = 'agp8-functional-test'
include ':app', ':feature', ':java-services', ':external-producer', ':external-bridge'
